services:
  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    ports:
      - "3000:3000"
    environment:
      KARDASHEV_API_URL: ${KARDASHEV_API_URL:-https://data.kardashevlabs.org}
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://lmp.kardashevlabs.org}
